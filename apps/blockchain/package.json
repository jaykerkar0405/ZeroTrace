{
  "private": true,
  "version": "1.0.0",
  "name": "@zerotrace/blockchain",
  "description": "Smart contracts for ZeroTrace quadratic funding platform",
  "scripts": {
    "test": "hardhat test",
    "clean": "hardhat clean",
    "build": "hardhat compile",
    "type-check": "tsc --noEmit",
    "lint": "prettier --check .",
    "format": "prettier --write .",
    "deploy": "hardhat run scripts/deploy.ts",
    "deploy:verifier": "hardhat run scripts/deploy-verifier.ts",
    "deploy:voting": "hardhat run scripts/deploy-voting.ts",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:verifier:sepolia": "hardhat run scripts/deploy-verifier.ts --network sepolia",
    "deploy:voting:sepolia": "hardhat run scripts/deploy-voting.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "@nomicfoundation/hardhat-ignition": "^0.15.8",
    "@nomicfoundation/hardhat-ignition-ethers": "^0.15.8",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.12",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.11",
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.8",
    "@typechain/ethers-v6": "^0.5.1",
    "@typechain/hardhat": "^9.1.0",
    "@types/node": "^22.19.8",
    "chai": "^4.5.0",
    "dotenv": "^17.2.4",
    "ethers": "^6.16.0",
    "hardhat": "^2.22.18",
    "hardhat-gas-reporter": "^1.0.10",
    "mocha": "^10.8.2",
    "prettier": "^3.4.2",
    "solidity-coverage": "^0.8.14",
    "typechain": "^8.3.2",
    "typescript": "~5.8.3"
  }
}
